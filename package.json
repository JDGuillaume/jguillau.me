{
  "name": "jguillaume-blog",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "pnpm run build && wrangler dev",
    "astro": "astro",
    "cf-typegen": "wrangler types",
    "deploy": "pnpm run build && wrangler deploy",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "test": "vitest run",
    "test:ci": "vitest run --coverage --reporter=junit --outputFile=test-results.xml",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "@astrojs/mdx": "^4.3.14",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "@vitest/ui": "^4.1.8",
    "astro": "^6.4.6",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@sugarcube-sh/vite": "^0.0.4",
    "@vitest/coverage-v8": "^4.1.8",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.69.0",
    "vitest": "^4.1.8",
    "wrangler": "^4.100.0"
  }
}
